server {
    listen 80;
    server_name _;

    # location /api {
    #   proxy_pass http://api:5001;
    #   include proxy.conf;
    # }

    location / {
      proxy_pass http://app:3000;
      include proxy.conf;
    }
}
